class BillingService:
    def reserve_upload_recognition(self, user_id: str, upload_id: str) -> None:
        # Development no-op. Production can reserve credits before running paid workflows.
        return None
